先进入 Grub界面，使用c => videoinfo 看看有什么分辨率
/etc/default/grub
# 控制亮度
GRUB_CMDLINE_LINUX_DEFAULT="loglevel=3 quiet acpi_backlight=video"
GRUB_GFXMODE=1024x768x32,auto
# sudo grub-mkconfig -o /boot/grub/grub.cfg
